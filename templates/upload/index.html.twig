{% extends 'base.html.twig' %}

{% block title %}Hello UploadController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <div class="container">
        <h1>GED 1</h1>
        <a href="{{ path('app_upload_list_type',{type: 'image'}) }}">
        <button class="btn btn-info">Images</button>
        </a>
        <a href="{{ path('app_upload_list_type',{'type': 'video'}) }}">
        <button class="btn btn-info">Video</button>
        </a>
        <a href="{{ path('app_upload_list_type',{'type': 'pdf'}) }}">
        <button class="btn btn-info">Document(Pdf)</button>
        </a>
        <a href="{{ path('app_upload_list_type',{'type': 'divers'}) }}">
        <button class="btn btn-info">Divers</button>
        </a>
    </div> 
</div>
{% endblock %}
